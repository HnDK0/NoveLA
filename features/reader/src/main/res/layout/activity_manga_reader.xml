<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/mangaReaderRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!-- Контейнер вьюера (пейджер/лента); зум/фильтры применяются к нему. -->
    <FrameLayout
        android:id="@+id/mangaViewerContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Compose-слой: тулбар, индикатор страницы, оверлей цвета, шит настроек. -->
    <androidx.compose.ui.platform.ComposeView
        android:id="@+id/mangaComposeHost"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="false"
        android:focusable="false" />

</FrameLayout>